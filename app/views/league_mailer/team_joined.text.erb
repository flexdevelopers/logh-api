<%= "#{@team.name} has joined #{@league.name} at LoseOrGoHome.com" %>.

<%= "Coach(es): #{@team.coach_names.join(', ')}" %>

<%= "https://www.loseorgohome.com/#/season/#{@league.season_id}/league/#{@league.id}" %>
