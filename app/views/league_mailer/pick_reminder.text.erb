<% if @league.max_picks_per_week  %>
<%= "This is your friendly reminder to get your #{pluralize(@league.max_picks_per_week, 'pick')} in for #{@week.name} for the \"#{@league.name}\" league (#{@league.season.name})." %>
<% else %>
<%= "This is your friendly reminder to get your picks in for #{@week.name} for the \"#{@league.name}\" league (#{@league.season.name})." %>
<% end %>

Don't worry. You can always make a pick before the start of any game. Good luck, loser!

*****

<%= "http://www.loseorgohome.com/#/season/#{@league.season_id}/league/#{@league.id}" %>
