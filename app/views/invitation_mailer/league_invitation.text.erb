Congrats! <%= @user.display_name %> has invited you to join the <%= @invitation.league.name %> league at loseorgohome.com!

<% if @invitation.message && @invitation.message.length > 0  %>
A message was also included:

<%= @invitation.message %>

*****

<% end %>
Join now and pick one NFL loser every week. No intelligence required. Follow this link to join the fun:

<%= "https://www.loseorgohome.com/#/season/#{@invitation.league.season_id}/league/#{@invitation.league.id}" %>
