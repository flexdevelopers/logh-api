<%= @requestor_email %> would like an invitation to the <%= @league.name %> private league.

<% if @requestor_message && @requestor_message.length > 0  %>
A message was also attached:

<%= @requestor_message %>

*****

<% end %>
If you are willing to extend an invitation to <%= @requestor_email %>, go to the league page and select 'Invite a coach to join':

<%= "https://www.loseorgohome.com/#/season/#{@league.season_id}/league/#{@league.id}" %>

