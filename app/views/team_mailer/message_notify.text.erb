The commish of the <%= @team.league.name %> league has left a message for your team, <%= @team.name %>:

<%= @team.message %>

*****

If you need to contact the commish you can do so on the team page:

<%= "http://www.loseorgohome.com/#/season/#{@team.league.season_id}/league/#{@team.league.id}/team/#{@team.id}" %>
