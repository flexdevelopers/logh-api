The commish of <%= @team.league.name %> has left a private message for <%= @team.name %>:

<%= @team.message %>

*****

If you need to contact the commish you can do so on your team page:

<%= "https://www.loseorgohome.com/#/season/#{@team.league.season_id}/league/#{@team.league.id}/team/#{@team.id}" %>
